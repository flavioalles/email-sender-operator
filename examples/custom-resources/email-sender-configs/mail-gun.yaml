# apiVersion: v1
# kind: Secret
# metadata:
#   name: mail-gun-credentials
#   namespace: dev
# type: kubernetes.io/basic-auth
# stringData:
#   # NOTE: might be unsuitable as username
#   # If so, it should become an Opaque secret
#   # containing only the API token.
#   username: flavio.alles@proton.me
#   password: an-api-token
---
apiVersion: "stable.email-sender-operator.dev/v1"
kind: EmailSenderConfig
metadata:
  name: mail-gun
  namespace: dev
spec:
  apiToken: an-api-token
  senderEmail: flavio.alles@proton.me
